# AUR packages (requires an AUR helper like yay or paru)
#
# Note: The install script will automatically install yay if needed
# Then install packages with:
#   yay -S --needed - < packages/aur.txt
#   or: paru -S --needed - < packages/aur.txt
#
# This file contains core AUR packages for TUI-first system
# For GUI-specific AUR packages, see gui-essential-aur.txt
# For communication apps, see gui-communication.txt

# Themes (used by both GUI and some TUI apps)
catppuccin-gtk-theme-frappe
catppuccin-cursors-frappe
catppuccin-sddm-theme-frappe

# System utilities (TUI)
pacseek          # TUI package manager
bluetuith-bin    # TUI Bluetooth manager
task             # TUI task management

# Wallpaper management (CLI)
quickwall        # CLI tool for downloading wallpapers from Unsplash
